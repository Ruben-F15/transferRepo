services:
#::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
  transfer-mongo:
    image: mongo:7
    container_name: transfer-mongo
    ports:
      - "27020:27017"
    restart: always
    volumes:
      - transfer_mongo:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASS}
    networks:
      - microservices-net
    healthcheck:
      test: [ "CMD", "mongosh", "-u", "${MONGO_ROOT_USER}", "-p", "${MONGO_ROOT_PASS}", "--authenticationDatabase", "admin", "--eval", "db.adminCommand('ping').ok" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

#::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
  transfer-mongo-init:
    image: mongo:7
    container_name: transfer-mongo-init
    depends_on:
      transfer-mongo:
        condition: service_healthy
    volumes:
      - ./mongo-init:/scripts
    entrypoint: [ "bash", "/scripts/init.sh" ]
    environment:
      MONGO_ROOT_USER: ${MONGO_ROOT_USER}
      MONGO_ROOT_PASS: ${MONGO_ROOT_PASS}
      APP_USER: ${MONGO_USER}
      APP_PASS: ${MONGO_PASS}
      MONGO_DB: ${MONGO_DB}
    networks:
      - microservices-net

  #::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
  transfer-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: transfer-service
    depends_on:
      transfer-mongo-init:
        condition: service_completed_successfully
    restart: always
    ports:
      - "8082:8080"
    environment:
      SPRING_MONGODB_URI: mongodb://${MONGO_USER}:${MONGO_PASS}@${MONGO_HOSTS}/${MONGO_DB}?authSource=${MONGO_DB}
      JWT_SECRET: ${SECRET_KEY}
    networks:
      - microservices-net


#::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
volumes:
  transfer_mongo:

networks:
  microservices-net:
    external: true