spring:
  application:
    name: transferservice

  # mongodb:
  #  uri: mongodb://localhost:27018/test?replicaSet=rs0


  data:
    mongodb:
      auto-index-creation: true

  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false

server:
  port: 8080


jwt:
  secret: "my-super-secret-key-for-testing-which-is-long-enough-123456"